/// block size for pixels in the world
pub const BASE_PIXEL_BLOCK_SIZE: i32 = 16;
pub const RENDER_SCALE: i32 = 4;

pub const BASE_RENDER_BLOCK_SIZE: i32 = BASE_PIXEL_BLOCK_SIZE * RENDER_SCALE;

pub const DEFAULT_GRAVITY = BASE_RENDER_BLOCK_SIZE * 20;
